# File Metadata API

## API

Post a file to the microservice to get the name, size and mimetype metadata

## Example

/api/upload

returns JSON

{"name":"file.txt", "type":"text/plain", "size":"15"}
on route

/api/filemeta
